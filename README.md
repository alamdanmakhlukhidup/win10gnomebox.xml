# win10gnomebox.xml

export EDITOR=nano
virsh edit win10

rendernode='/dev/dri/renderD128'

use : new windows when creating it

what do we change? virtio driver for eveything
and change open gl and 3d acceleration to yes
use sata but driver cache change it to the provided xml

download : spice window driver first tho
and lat : download spice for folder sharing

if no gpu no trackpad gestures
if cpu only needs lots of ram and no image rendering
